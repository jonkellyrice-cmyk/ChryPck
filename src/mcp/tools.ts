export const CHRYPCK_TOOL_NAMES=["chrypck_plan","chrypck_context","chrypck_execute","chrypck_result"] as const;
export type ChryPckToolName=typeof CHRYPCK_TOOL_NAMES[number];
export const CHRYPCK_TOOLS=Object.freeze([
{name:"chrypck_plan",readOnly:true,description:"Start governed repository work. ChryPck builds global deterministic structure immediately and lazily requests at most one objective-local semantic region when navigation reaches an unmapped boundary. Global Semantic Atlas completion is never required. Choose analysis.kind='trace' for one causal runtime route or analysis.kind='dataflow-slice' for bounded value provenance/influence. Usable focused analysis is persisted for analysis_handoff; trace_handoff remains compatible. Semantic and analysis evidence never independently grants mutation authority or exact-source access."},
{name:"chrypck_context",readOnly:true,description:"Read only server-certified Context Pack evidence from a READY normal-plan run. The bounded index includes Contract Map provider/consumer evidence and Effect / Runtime Atlas roles, effect families, reconciliation and verification-target evidence. Focused-analysis runs expose no arbitrary exact source; use certified analysis_handoff to create a distinct normal plan. trace_handoff remains compatible for Trace clients. Arbitrary paths are never accepted."},
{name:"chrypck_execute",readOnly:false,description:"Mutate an existing READY normal-plan run with objective-sufficient semantic coverage using exactly one mode: user-authorized typed bounded authoring_intent or explicit architecture approval. Effect / Runtime Atlas, Contract Map and analysis lineage inform but never widen execution authority. ChryPck performs runtime/contract-aware propagation, validation and atomic publication."},
{name:"chrypck_result",readOnly:true,description:"Read authoritative bounded run state after bootstrap, planning, Trace, Dataflow Slice, execution or failure. Results expose persisted focused-analysis artifacts or accepted analysis lineage, bounded maps, propagation/validation evidence, telemetry, publication state and next permitted action."}
] as const);
