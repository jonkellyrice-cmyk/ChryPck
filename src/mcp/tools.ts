export const CHRYPCK_TOOL_NAMES=["chrypck_plan","chrypck_context","chrypck_execute","chrypck_result"] as const;
export type ChryPckToolName=typeof CHRYPCK_TOOL_NAMES[number];
export const CHRYPCK_TOOLS=Object.freeze([
{name:"chrypck_plan",readOnly:true,description:"Start governed repository work. After mandatory bounded Semantic Atlas bootstrap, ChryPck returns Structural/Semantic orientation, compressed diagnostics, the canonical bounded Effect / Runtime Atlas, reconciled Contract Map, certified Patch Corridor and initial Contract/Runtime-aware Context Pack. Effect / Runtime Atlas maps possible behavior; analysis.kind='trace' certifies one focused causal route and is persisted for explicit trace_handoff into a distinct normal plan. Atlas, Contract Map and Trace evidence can strengthen existing repository evidence but never independently grants mutation authority or exact-source access."},
{name:"chrypck_context",readOnly:true,description:"Read only server-certified Context Pack evidence from a READY normal-plan run. The bounded index includes Contract Map provider/consumer evidence and Effect / Runtime Atlas roles, effect families, reconciliation and verification-target evidence. A Trace run itself exposes no arbitrary exact source; use certified trace_handoff to create a distinct normal plan. Arbitrary paths are never accepted."},
{name:"chrypck_execute",readOnly:false,description:"Mutate an existing fully bootstrapped READY normal-plan run using exactly one mode: user-authorized typed bounded authoring_intent or explicit architecture approval. Effect / Runtime Atlas, Contract Map and Trace lineage inform but never widen execution authority. ChryPck performs runtime/contract-aware propagation, validation and atomic publication."},
{name:"chrypck_result",readOnly:true,description:"Read authoritative bounded run state after bootstrap, planning, Trace, execution or failure. Results expose the bounded Effect / Runtime Atlas, reconciled Contract Map, persisted Trace artifact or trace_handoff lineage, runtime and contract propagation/validation evidence, telemetry, publication state and next permitted action."}
] as const);
