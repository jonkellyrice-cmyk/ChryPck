export const CHRYPCK_TOOL_NAMES=["chrypck_plan","chrypck_context","chrypck_execute","chrypck_result"] as const;
export type ChryPckToolName=typeof CHRYPCK_TOOL_NAMES[number];
export const CHRYPCK_TOOLS=Object.freeze([
{name:"chrypck_plan",readOnly:true,description:"Start governed repository work. ChryPck snapshots the repository and builds its exhaustive hidden Repository Model. On the first uncached plan for a repository commit/profile it intentionally stops before ordinary diagnostics and returns one bounded Semantic Atlas bootstrap metadata chunk; the host LLM must interpret every region with server-issued evidence_refs and resubmit via chrypck_plan.semantic_bootstrap until bootstrap is complete. After that mandatory first-pass orientation, chrypck_plan returns Structural/Semantic Atlases and coverage, compressed dependency/runtime/symbol/state/native-contract diagnostics, a certified Patch Corridor and initial Context Pack, and optional focused analysis. For bugs/runtime/dependency/callback/state/effect investigation use analysis.kind='trace'. Trace is the single bounded evidence-backed BEFT engine and is persisted as an authoritative run artifact with a certificate when possible. If Trace returns permitted_next_action='create_normal_plan_with_trace_handoff', call chrypck_plan again as a new normal plan with trace_handoff {run_id, optional certificate_id}; do not manually paraphrase Trace into planning when certified lineage is available. ChryPck verifies that the Trace belongs to the same repository, immutable commit and project profile, then uses only the certified Trace path as bounded planning evidence while independently re-certifying the Patch Corridor. Trace/trace_handoff never grants mutation authority or exact-source access by itself."},
{name:"chrypck_context",readOnly:true,description:"Read only server-certified Context Pack evidence from an existing READY normal-plan run after any required Semantic Atlas bootstrap and planning are complete. A Trace run itself does not expose exact-source context; first create a normal plan, using trace_handoff when certified Trace evidence should inform it. Omit segment_id for the certified context index or use a server-issued segment_id for one bounded exact-source expansion. Arbitrary paths are never accepted."},
{name:"chrypck_execute",readOnly:false,description:"Mutate an existing fully bootstrapped READY normal-plan run using exactly one mode: a user-authorized typed bounded authoring_intent containing the coherent patch across all newly certified touch points, or explicit approval of a server-issued architecture plan. Trace/trace_handoff evidence may inform planning but never widens mutation authority by itself. ChryPck stages the full change, performs dependency-aware Change Propagation, validates broadly, and publishes atomically."},
{name:"chrypck_result",readOnly:true,description:"Read the authoritative bounded run state and outcome after semantic bootstrap/planning, tracing, execution, or failure. Trace runs retain their authoritative Trace artifact and certificate here; normal plans report accepted trace_handoff lineage. Use this result to verify terminal status, semantic orientation, diagnostics, propagation/validation evidence, telemetry, publication state, and the next permitted action rather than inferring success from an earlier call."}
] as const);
