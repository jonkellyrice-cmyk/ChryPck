export const CHRYPCK_TOOL_NAMES=["chrypck_plan","chrypck_context","chrypck_execute","chrypck_result"] as const;
export type ChryPckToolName=typeof CHRYPCK_TOOL_NAMES[number];
export const CHRYPCK_TOOLS=Object.freeze([
{name:"chrypck_plan",readOnly:true,description:"Start governed repository work. ChryPck snapshots the repository and builds its exhaustive hidden Repository Model. On the first uncached plan for a repository commit/profile it intentionally stops before ordinary diagnostics and returns one bounded Semantic Atlas bootstrap metadata chunk; the host LLM must interpret every region with server-issued evidence_refs and resubmit via chrypck_plan.semantic_bootstrap until bootstrap is complete. After that mandatory first-pass orientation, chrypck_plan returns the Structural Repository Atlas plus coverage, the evidence-backed Semantic Atlas plus semantic coverage, dependency graph/watershed, symbol-family, effect-atlas, integration-surface, runtime-signal, state-namespace, native-contract and project-profile diagnostics, a certified Patch Corridor and initial Context Pack, and optional focused analysis. Use Semantic Atlas for purpose/responsibility/boundary orientation, Structural Atlas for location, diagnostics for actual relationships, and certified source only where needed. For bug, runtime-flow, handler-chain, dependency-flow, callback, state, or effect investigation use analysis.kind='trace'. Trace is always the bounded evidence-backed BEFT engine: sourceSymbol and targetEffect are optional when ChryPck can resolve an evidence-supported entrypoint from the objective; explicit source/target values narrow the trace. Trace supports bounded hop/branch controls plus optional file/symbol filters, returns blocker/effect/path evidence and a path certificate when possible, and never falls back to an arbitrary graph node. Semantic metadata is inferential orientation and never mutation authority."},
{name:"chrypck_context",readOnly:true,description:"Read only server-certified Context Pack evidence from an existing READY run after any required Semantic Atlas bootstrap and normal planning complete. Omit segment_id for the certified context index or use a server-issued segment_id for one bounded exact-source expansion. Use this after semantic/structural orientation plus planning/tracing identify the relevant touch points but exact source is needed to understand or author the complete cross-file patch; arbitrary paths are never accepted."},
{name:"chrypck_execute",readOnly:false,description:"Mutate an existing fully bootstrapped and planned run using exactly one mode: a user-authorized typed bounded authoring_intent containing the coherent patch across all certified touch points, or explicit approval of a server-issued architecture plan. ChryPck stages the full change, performs dependency-aware Change Propagation, validates broadly, and publishes atomically. Semantic Atlas metadata never expands the Patch Corridor or mutation authority. Do not use this for read-only diagnosis."},
{name:"chrypck_result",readOnly:true,description:"Read the authoritative bounded run state and outcome after semantic bootstrap/planning, tracing, execution, or failure. Use it to verify terminal status, semantic orientation when available, diagnostics, propagation/validation evidence, telemetry, publication state, and the next permitted action rather than inferring success from an earlier call."}
] as const);
