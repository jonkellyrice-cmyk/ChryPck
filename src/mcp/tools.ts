export const CHRYPCK_TOOL_NAMES=["chrypck_plan","chrypck_context","chrypck_execute","chrypck_result"] as const;
export type ChryPckToolName=typeof CHRYPCK_TOOL_NAMES[number];
export const CHRYPCK_TOOLS=Object.freeze([
{name:"chrypck_plan",readOnly:true,description:"Start governed repository work. ChryPck snapshots the repository, builds its exhaustive hidden Repository Model, runs dependency graph/watershed, symbol-family, effect-atlas, integration-surface, runtime-signal, state-namespace, native-contract and project-profile diagnostics, certifies a Patch Corridor and initial Context Pack, and optionally performs focused analysis. For bug/runtime-flow investigation use analysis.kind='trace'; when a source symbol or target effect is known use analysis.kind='bounded-event-trace' with sourceSymbol, optional targetEffect, and bounded hop/branch options. For deliberate architecture work, architecture modes remain read-only until an explicit execute step."},
{name:"chrypck_context",readOnly:true,description:"Read only server-certified Context Pack evidence from an existing READY run. Omit segment_id for the certified context index or use a server-issued segment_id for one bounded exact-source expansion. Use this after planning/tracing when compressed diagnostics identify the relevant touch points but exact source is needed to understand or author the complete cross-file patch; arbitrary repository paths are never accepted."},
{name:"chrypck_execute",readOnly:false,description:"Mutate an existing planned run using exactly one user-authorized operation: a typed bounded authoring_intent containing the coherent patch across all certified touch points, or explicit approval of a server-issued architecture plan. ChryPck stages the full change, performs dependency-aware Change Propagation, validates broadly, and publishes atomically. Do not use this for read-only diagnosis."},
{name:"chrypck_result",readOnly:true,description:"Read the authoritative bounded run state and outcome after planning, tracing, execution, or failure. Use it to verify terminal status, diagnostics, propagation/validation evidence, telemetry, publication state, and the next permitted action rather than inferring success from an earlier call."}
] as const);
