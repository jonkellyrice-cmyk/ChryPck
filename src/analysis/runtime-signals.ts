import type { RepositoryModel } from "../repository/model.js";
import { freezeResult, type Analyzer, type DiagnosticFinding } from "./analyzer.js";
import { buildEffectRuntimeAtlas } from "./effect-runtime-linker.js";
const SIGNAL_KINDS=new Set(["hooks","chat-output","notifications","application-lifecycle","native-execution"]);
export const runtimeSignalsAnalyzer:Analyzer<DiagnosticFinding>={name:"runtime-signal-map",analyze(model:RepositoryModel){const atlas=buildEffectRuntimeAtlas(model),signals=model.effects.filter(e=>SIGNAL_KINDS.has(e.kind));const findings=signals.map(signal=>({code:"RUNTIME_SIGNAL",severity:"info" as const,summary:`${signal.kind} signal at ${signal.file}:${signal.line}.`,evidence:{kind:signal.kind,file:signal.file,line:signal.line,symbol:signal.symbol,detail:signal.detail,canonical_atlas:"effect-runtime-atlas"}}));return freezeResult(this.name,{signals:signals.length,kinds:new Set(signals.map(s=>s.kind)).size,files:new Set(signals.map(s=>s.file)).size,observable_effects:atlas.coverage.observableEffects,compatibility_projection:true},findings);}};
