import type { AbstractionLock } from "../policy/abstraction-lock.js";import type { ScopeLock } from "../policy/scope-lock.js";import type { RequestEnvelope,RequestIdentity } from "./request-envelope.js";import type { OrchestratorStateRecord,RunTelemetry } from "./telemetry.js";import type { RunState } from "./run-state.js";
export interface NativeToolchainRun{readonly runId:string;readonly repository:string;readonly requestPath:string;readonly requestIdentity:RequestIdentity;readonly envelope:RequestEnvelope;readonly scopeLock:ScopeLock;readonly abstractionLock:AbstractionLock;state:RunState;stateRecord:OrchestratorStateRecord;readonly telemetry:RunTelemetry;requestCommitSha:string|null;resultCommitSha:string|null}
export class RunStore{readonly #runs=new Map<string,NativeToolchainRun>();readonly #byRequest=new Map<string,string>();private requestKey(repository:string,fingerprint:string):string{return`${repository}@${fingerprint}`}put(run:NativeToolchainRun):NativeToolchainRun{this.#runs.set(run.runId,run);this.#byRequest.set(this.requestKey(run.repository,run.requestIdentity.fingerprint),run.runId);return run}get(runId:string):NativeToolchainRun|null{return this.#runs.get(runId)??null}find(repository:string,fingerprint:string):NativeToolchainRun|null{const runId=this.#byRequest.get(this.requestKey(repository,fingerprint));return runId?this.get(runId):null}require(runId:string):NativeToolchainRun{const run=this.get(runId);if(!run)throw new Error(`Unknown ChryPck run: ${runId}`);return run}snapshot():readonly NativeToolchainRun[]{return Object.freeze([...this.#runs.values()])}}
