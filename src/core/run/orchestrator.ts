import { createHash } from "node:crypto";
import { createDefaultAbstractionLock } from "../policy/abstraction-lock.js";
import { assertScopeLock } from "../policy/scope-lock.js";
import { buildRequestEnvelope, type RequestIdentity } from "./request-envelope.js";
import { createNativeRunArtifacts, type NativeRunArtifacts } from "./run-artifacts.js";
import { RunStore, type NativeToolchainRun } from "./run-store.js";
import { assertTransition, isTerminal, type RunState } from "./run-state.js";
import { nativeFailureRecord,nativeSuccessRecord,RunTelemetry,stateRecord } from "./telemetry.js";
import type { NativeFailureEvidence } from "./failure-evidence.js";
export interface AdmitRequestInput{readonly repository:string;readonly request:Record<string,any>;readonly requestPath:string}
function runIdFor(repository:string,identity:RequestIdentity):string{return`chrypck-${createHash("sha256").update(`${repository}:${identity.fingerprint}`).digest("hex").slice(0,16)}`}
export class NativeOrchestrator{constructor(readonly store=new RunStore()){}admitRequest(input:AdmitRequestInput):NativeToolchainRun{const envelope=buildRequestEnvelope(input.request,input.requestPath),existing=this.store.find(input.repository,envelope.request.fingerprint);if(existing)return existing;const scopeLock=assertScopeLock(input.request),abstractionLock=createDefaultAbstractionLock(scopeLock),runId=runIdFor(input.repository,envelope.request),telemetry=new RunTelemetry(runId);const run:NativeToolchainRun={runId,repository:input.repository,requestPath:input.requestPath,requestIdentity:envelope.request,envelope,scopeLock,abstractionLock,state:"READY",stateRecord:stateRecord(envelope.request,"READY",{scope_lock:scopeLock}),telemetry,artifacts:createNativeRunArtifacts(),requestCommitSha:null,resultCommitSha:null};telemetry.record("READY","request_admitted",{requestFingerprint:envelope.request.fingerprint});return this.store.put(run)}bindRequestCommit(runId:string,commitSha:string|null):NativeToolchainRun{const run=this.store.require(runId);run.requestCommitSha=commitSha;run.telemetry.record(run.state,"request_commit_bound",{commitSha});return run}transition(runId:string,next:RunState,event:string=next):NativeToolchainRun{const run=this.store.require(runId);if(run.state===next)return run;if(isTerminal(run.state))throw new Error(`Run ${runId} is terminal.`);assertTransition(run.state,next);run.state=next;run.stateRecord=stateRecord(run.requestIdentity,next,{scope_lock:run.scopeLock});run.telemetry.record(next,event);return run}recordArtifact<K extends keyof NativeRunArtifacts>(runId:string,name:K,value:NativeRunArtifacts[K],summary:Readonly<Record<string,unknown>>={}):NativeToolchainRun{const run=this.store.require(runId);if(isTerminal(run.state))throw new Error(`Run ${runId} is terminal; artifacts are closed.`);run.artifacts[name]=value;run.telemetry.record(run.state,`artifact_${String(name)}`,summary);return run}failNative(runId:string,evidence:NativeFailureEvidence):NativeToolchainRun{const run=this.store.require(runId);if(isTerminal(run.state))return run;assertTransition(run.state,"FAILED");run.artifacts.failure=evidence;run.state="FAILED";run.stateRecord=nativeFailureRecord(run.requestIdentity,evidence);run.telemetry.record("FAILED","native_failure",{stage:evidence.failed_stage,failureClass:evidence.failure_class});return run}succeedNative(runId:string,resultCommitSha:string,validationFingerprint:string):NativeToolchainRun{const run=this.store.require(runId);if(isTerminal(run.state))throw new Error(`Run ${runId} is terminal.`);assertTransition(run.state,"SUCCEEDED");run.resultCommitSha=resultCommitSha;run.state="SUCCEEDED";run.stateRecord=nativeSuccessRecord(run.requestIdentity,resultCommitSha,validationFingerprint);run.telemetry.record("SUCCEEDED","native_success",{resultCommitSha,validationFingerprint});return run}}
export class RunController{constructor(readonly run:NativeToolchainRun){}transition(next:RunState,event:string=next):NativeToolchainRun{if(this.run.state===next)return this.run;if(isTerminal(this.run.state))throw new Error(`Run ${this.run.runId} is terminal.`);assertTransition(this.run.state,next);this.run.state=next;this.run.stateRecord=stateRecord(this.run.requestIdentity,next,{scope_lock:this.run.scopeLock});this.run.telemetry.record(next,event);return this.run}}
