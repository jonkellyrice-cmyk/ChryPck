import { createHash } from "node:crypto";
import { buildScopeLock, type ScopeLockResult } from "../policy/scope-lock.js";
export const REQUEST_ENVELOPE_SCHEMA_VERSION = 1;
export const DEFAULT_REQUEST_PATH = "dev_scripts/github-filepatcher.json";
export interface RequestIdentity { readonly id:string; readonly fingerprint:string; readonly fingerprint_short:string; readonly request_path:string; }
export interface RequestEnvelope {
 readonly schema_version:number; readonly kind:"frame_conn_request_envelope"; readonly request:RequestIdentity;
 readonly scope_lock:Readonly<{state:ScopeLockResult["state"];fingerprint:string|null;objective_fingerprint:string|null;original_user_instruction:string|null;authorized_deliverables:readonly string[];authority:ScopeLockResult["authority"]|null;violations:ScopeLockResult["violations"]}>;
 readonly intent:Readonly<{goal:string|null;acceptance_criteria:readonly string[];non_goals:readonly string[]}>;
 readonly scope:Readonly<{explicit:readonly string[];allowed_paths:readonly string[];operation_paths:readonly string[]}>;
 readonly evidence:readonly unknown[]; readonly manifest:Readonly<{artifacts:unknown;result:unknown}>;
 readonly source:Readonly<{request_path:string;authored_id:string|null;description:string|null;schema_version:unknown;operation_count:number}>;
 readonly authority:Readonly<{authoritative_for:readonly string[];not_authoritative_for:readonly string[]}>; readonly envelope_fingerprint:string;
}
function stable(value:unknown):unknown{if(Array.isArray(value))return value.map(stable);if(value&&typeof value==="object"){const record=value as Record<string,unknown>;return Object.fromEntries(Object.keys(record).sort().map(key=>[key,stable(record[key])]))}return value}
function hash(value:unknown):string{const serialized=JSON.stringify(stable(value));if(serialized===undefined)throw new TypeError("Request Envelope fingerprint input must be JSON-serializable.");return createHash("sha256").update(serialized,"utf8").digest("hex")}
function strings(value:unknown):string[]{const values=Array.isArray(value)?value:value==null?[]:[value];return [...new Set(values.map(entry=>String(entry).trim()).filter(Boolean))]}
function repoPath(value:unknown):string{return String(value??"").replaceAll("\\","/").replace(/^\.\//,"")}
export function semanticRequestProjection(request:Record<string,any>):unknown{const value:Record<string,any>={...(request??{})};for(const key of ["id","description","orchestrator","telemetry","execution_metadata","runtime_metadata","request_id","request_fingerprint","request_envelope","toolchain_artifacts","result","raw_operations_reason","native_run","last_applied_at","last_applied_by"])delete value[key];return stable(value)}
export function fingerprintRequest(request:Record<string,any>):string{return hash(semanticRequestProjection(request))}
export function buildRequestIdentity(request:Record<string,any>,requestPath=DEFAULT_REQUEST_PATH):RequestIdentity{const fingerprint=fingerprintRequest(request);const label=String(request?.id||"request").trim().replace(/[^A-Za-z0-9._-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,72)||"request";return{id:`${label}-${fingerprint.slice(0,12)}`,fingerprint,fingerprint_short:fingerprint.slice(0,12),request_path:repoPath(requestPath)}}
export function operationPaths(request:Record<string,any>):string[]{const output:string[]=[];for(const operation of request?.operations??[]){for(const key of ["path","from","to"] as const)if(operation?.[key])output.push(repoPath(operation[key]));for(const root of operation?.roots??[])output.push(repoPath(root))}for(const edit of request?.authoring_intent?.edits??[])if(edit?.path)output.push(repoPath(edit.path));for(const move of request?.moves??[]){if(move?.from)output.push(repoPath(move.from));if(move?.to)output.push(repoPath(move.to))}for(const candidate of request?.candidates??[]){if(candidate?.source)output.push(repoPath(candidate.source));for(const unit of candidate?.units??[])if(unit?.target)output.push(repoPath(unit.target))}return[...new Set(output.filter(Boolean))].sort()}
function artifacts(value:unknown):unknown{const source=value&&typeof value==="object"&&!Array.isArray(value)?value as Record<string,unknown>:{};return stable(Object.fromEntries(Object.entries(source).map(([name,item])=>[name,typeof item==="string"?{ref:item}:item])))}
export function buildRequestEnvelope(request:Record<string,any>,requestPath=DEFAULT_REQUEST_PATH,options:Readonly<{artifacts?:unknown;result?:unknown}>={}):RequestEnvelope{if(!request||typeof request!=="object"||Array.isArray(request))throw new TypeError("Request Envelope requires an object.");const identity=buildRequestIdentity(request,requestPath);const scopeLock=buildScopeLock(request);const envelopeWithoutFingerprint={schema_version:REQUEST_ENVELOPE_SCHEMA_VERSION,kind:"frame_conn_request_envelope" as const,request:identity,scope_lock:{state:scopeLock.state,fingerprint:scopeLock.fingerprint,objective_fingerprint:scopeLock.objective_fingerprint??null,original_user_instruction:scopeLock.original_user_instruction??null,authorized_deliverables:scopeLock.authorized_deliverables??[],authority:scopeLock.authority??null,violations:scopeLock.violations??[]},intent:{goal:String(request.planning_goal??request.goal??"").trim()||null,acceptance_criteria:strings(request.acceptance_criteria??request.acceptanceCriteria),non_goals:strings(request.non_goals??request.nonGoals)},scope:{explicit:strings(request.scope).map(repoPath).sort(),allowed_paths:strings(request?.policy?.allowed_paths).map(repoPath).sort(),operation_paths:operationPaths(request)},evidence:Array.isArray(request.evidence)?stable(request.evidence) as readonly unknown[]:[],manifest:{artifacts:artifacts(options.artifacts??request.toolchain_artifacts),result:options.result??request.result??null},source:{request_path:repoPath(requestPath),authored_id:typeof request.id==="string"?request.id:null,description:typeof request.description==="string"?request.description:null,schema_version:request.schema_version??null,operation_count:request.operations?.length??0},authority:{authoritative_for:["semantic_request_identity","scope_lock","original_user_instruction","request_goal","acceptance_criteria","non_goals","declared_scope","request_artifact_index"],not_authoritative_for:["architectural_ownership","patch_corridor_certification","mutation_operations","validation","promotion"]}};return Object.freeze({...envelopeWithoutFingerprint,envelope_fingerprint:hash(envelopeWithoutFingerprint)})}
