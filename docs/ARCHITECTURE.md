# ChryPck Native Toolchain Architecture

This directory will document the native MCP/toolchain architecture as behavior migrates out of `dev_scripts/`.

The scaffold is intentionally non-functional. Existing runtime behavior remains authoritative until each migration pass reaches parity and is switched over explicitly.
